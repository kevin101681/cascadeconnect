# Cascade Connect - Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local or any file with real secrets

# Neon Auth Configuration
# Get these from your Neon dashboard > Auth section
VITE_NEON_AUTH_URL=          # Base URL from Neon dashboard
VITE_NEON_AUTH_JWKS_URL=     # JWKS URL from Neon dashboard (optional, may be derived from base URL)

# Clerk Authentication (DEPRECATED - Replaced by Stack Auth)
# VITE_CLERK_PUBLISHABLE_KEY=

# Database Connection
# IMPORTANT: For browser/client-side access, use VITE_DATABASE_URL
# Get this from your Neon database dashboard > Connection String
# Format: postgresql://user:password@host/database?sslmode=require
VITE_DATABASE_URL=

# Alternative: Server-Side Only (if using API proxy)
# DATABASE_URL=

# Gemini AI API Key (for BlueTag Punch List AI features)
# Get this from https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=

# UploadThing Configuration (Server-Side Only)
# Get these from your UploadThing dashboard
UPLOADTHING_APP_ID=
UPLOADTHING_SECRET=

# Cloudinary Configuration (Server-Side Only)
# Get these from your Cloudinary dashboard: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Gemini AI (Optional - for AI features)
# Get this from Google AI Studio
VITE_GEMINI_API_KEY=

# Server Port (Optional)
PORT=3000

# CBS Books Database Connection (Neon)
# Use the same database URL from your standalone CBS Books app
NETLIFY_DATABASE_URL=
# Alternative: DATABASE_URL=

# CBS Books Square Payment Configuration (Optional)
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=production

# Email Configuration - Choose ONE method:
# Option 1: SMTP (Generic - works with any SMTP provider)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Option 2: SendGrid (Recommended for automatic email reply handling)
# Get API key from https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=
# The email address that will receive replies (e.g., replies@yourdomain.com)
# This should match the domain configured in SendGrid Inbound Parse
SENDGRID_REPLY_EMAIL=

# CBS Books Migration (Temporary - for migration script only)
# Source database URL (your standalone CBS Books database)
DATABASE_URL_SOURCE=
# Alternative: CBS_BOOKS_DATABASE_URL=
# Target database URL (your Cascade Connect database - usually same as VITE_DATABASE_URL)
DATABASE_URL_TARGET=

